export enum SupportedChains {
  ethereum = 'ethereum',
  optimism = 'optimism',
  polygon = 'polygon',
}

export enum TransactionState {
  pending = 'pending',
  failed = 'failed',
  complete = 'complete',
  notStarted = 'notStarted',
}
